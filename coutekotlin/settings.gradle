import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)
    repositories {
        google()
        mavenCentral()
    }
}

include ':app'
rootProject.name = 'count_android'                                // assumed existing content
setBinding(new Binding([gradle: this]))                                // new
evaluate(new File(settingsDir.parentFile, '/countflutter/.android/include_flutter.groovy'))
